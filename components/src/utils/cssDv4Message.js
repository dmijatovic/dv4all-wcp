/**
 * dv4-message shared class
 * this class is used bu inputs and select element
 */
export default `
/* MESSAGE */
.dv4-message{
  padding: var(--message-padding, 0.25rem);
  font-size: calc(var(--message-font-size, 1rem) * 0.75);
  line-height: var(--message-font-size, 1rem);
}
`
